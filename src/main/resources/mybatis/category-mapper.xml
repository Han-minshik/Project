<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.CategoryMapper">
    <select id="findCategoryByNameAndParent" resultType="java.lang.Integer">
        SELECT no
        FROM category
        WHERE name = #{name} AND (parent = #{parent} OR (#{parent} IS NULL AND parent IS NULL))
    </select>
    <insert id="insertCategory" parameterType="com.project.dto.CategoryDTO" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO category (name, parent, level)
        VALUES (#{name}, #{parent}, #{level})
    </insert>
</mapper>
